name: Agent Efficiency
on:
  # Customize trigger events, filters, and schedules for your repo.
  schedule:
    - cron: "0 16 * * 1-5"  # adjust schedule
  workflow_dispatch:  # enable manual runs

permissions:
  # Use least-privilege; add write scopes only when needed.
  actions: read
  contents: read
  issues: write
  pull-requests: read

jobs:
  run:
    # Pin to a release tag; update when upgrading.
    uses: elastic/ai-github-actions/.github/workflows/gh-aw-agent-efficiency.lock.yml@v0
    # Required: COPILOT_GITHUB_TOKEN secret.
    secrets:
      COPILOT_GITHUB_TOKEN: ${{ secrets.COPILOT_GITHUB_TOKEN }}
