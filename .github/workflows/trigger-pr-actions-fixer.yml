# This file is auto-generated by scripts/dogfood.sh. Do not edit directly.
# Edit gh-agent-workflows/pr-actions-fixer/example.yml and run 'make compile' to regenerate.
name: Trigger PR Actions Fixer
on:
  workflow_dispatch:
    inputs:
      workflow-run-id:
        description: "Failed workflow run ID to analyze"
        required: true
        type: string

permissions:
  actions: read
  contents: write
  issues: write
  pull-requests: write

jobs:
  run:
    uses: ./.github/workflows/gh-aw-pr-actions-fixer.lock.yml
    with:
      workflow-run-id: ${{ inputs.workflow-run-id }}
    secrets:
      COPILOT_GITHUB_TOKEN: ${{ secrets.COPILOT_GITHUB_TOKEN }}
