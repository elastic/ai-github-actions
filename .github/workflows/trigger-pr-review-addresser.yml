# This file is auto-generated by scripts/dogfood.sh. Do not edit directly.
# Edit gh-agent-workflows/pr-review-addresser/example.yml and run 'make compile' to regenerate.
name: Trigger PR Review Addresser
on:
  pull_request_review:
    types: [submitted]

permissions:
  actions: read
  contents: write
  issues: write
  pull-requests: write

jobs:
  run:
    if: >-
      (github.event.review.state == 'changes_requested' || github.event.review.state == 'commented') &&
      !github.event.pull_request.draft &&
      !contains(github.event.pull_request.labels.*.name, 'skip-auto-pr-review-addresser')
    uses: ./.github/workflows/gh-aw-pr-review-addresser.lock.yml
    secrets:
      COPILOT_GITHUB_TOKEN: ${{ secrets.COPILOT_GITHUB_TOKEN }}
