# This file is auto-generated by scripts/dogfood.sh. Do not edit directly.
# Edit gh-agent-workflows/stale-issues-investigator/example.yml and run 'make compile' to regenerate.
name: Trigger Stale Issues Investigator
on:
  schedule:
    - cron: "0 15 * * 1-5"
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  copilot-requests: write
  issues: write
  pull-requests: write

jobs:
  run:
    uses: ./.github/workflows/gh-aw-stale-issues-investigator.lock.yml
