# This file is auto-generated by scripts/dogfood.sh. Do not edit directly.
# Edit gh-agent-workflows/code-duplication-detector/example.yml and run 'make compile' to regenerate.
name: Trigger Code Duplication Detector
on:
  schedule:
    - cron: "0 12 * * 1-5"
  workflow_dispatch:
    inputs:
      issue-labels:
        description: "Comma-separated labels to apply when creating an issue"
        required: false
        default: "code-duplication,ai:fix-ready"
      assign-to:
        description: "Comma-separated GitHub usernames to assign when creating an issue"
        required: false
        default: ""

permissions:
  contents: read
  issues: write
  pull-requests: read

jobs:
  run:
    uses: ./.github/workflows/gh-aw-code-duplication-detector.lock.yml
    with:
      languages: "go,python,typescript"
      additional-instructions: |
        When calling create_issue, apply these labels:
        `${{ inputs.issue-labels || 'code-duplication,ai:fix-ready' }}`
        If `${{ inputs.assign-to || '' }}` is non-empty, set create_issue assignees to those usernames.
    secrets:
      COPILOT_GITHUB_TOKEN: ${{ secrets.COPILOT_GITHUB_TOKEN }}
