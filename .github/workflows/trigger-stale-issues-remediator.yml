# This file is auto-generated by scripts/dogfood.sh. Do not edit directly.
# Edit gh-agent-workflows/stale-issues-remediator/example.yml and run 'make compile' to regenerate.
name: Trigger Stale Issues Remediator
on:
  schedule:
    - cron: "0 16 * * 1-5"
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  run:
    uses: ./.github/workflows/gh-aw-stale-issues-remediator.lock.yml
    secrets:
      COPILOT_GITHUB_TOKEN: ${{ secrets.COPILOT_GITHUB_TOKEN }}
