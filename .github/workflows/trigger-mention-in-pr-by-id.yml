# This file is auto-generated by scripts/dogfood.sh. Do not edit directly.
# Edit gh-agent-workflows/mention-in-pr-by-id/example.yml and run 'make compile' to regenerate.
name: Trigger Mention in PR by ID
on:
  workflow_dispatch:
    inputs:
      pull-request-number:
        description: "PR number to target (e.g. 123)"
        required: true
        type: string
      prompt:
        description: "Prompt for the agent (review/fix/explain/etc.)"
        required: true
        type: string

permissions:
  actions: read
  contents: write
  discussions: write
  issues: write
  pull-requests: write

jobs:
  run:
    uses: ./.github/workflows/gh-aw-mention-in-pr-by-id.lock.yml
    with:
      target-pr-number: ${{ inputs.pull-request-number }}
      prompt: ${{ inputs.prompt }}
    secrets:
      COPILOT_GITHUB_TOKEN: ${{ secrets.COPILOT_GITHUB_TOKEN }}
      EXTRA_COMMIT_GITHUB_TOKEN: ${{ secrets.EXTRA_COMMIT_GITHUB_TOKEN }}
