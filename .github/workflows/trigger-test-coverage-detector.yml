# This file is auto-generated by scripts/dogfood.sh. Do not edit directly.
# Edit gh-agent-workflows/test-coverage-detector/example.yml and run 'make compile' to regenerate.
name: Trigger Test Coverage Detector
on:
  schedule:
    - cron: "0 10 * * 1-5"
  workflow_dispatch:

permissions:
  actions: read
  contents: write
  copilot-requests: write
  issues: write
  pull-requests: write

jobs:
  run:
    uses: ./.github/workflows/gh-aw-test-coverage-detector.lock.yml  create_pr_from_issue:
    needs: run
    if: ${{ needs.run.outputs.created_issue_number != '' }}
    uses: ./.github/workflows/gh-aw-create-pr-from-issue.lock.yml
    with:
      target-issue-number: ${{ needs.run.outputs.created_issue_number }}
      additional-instructions: "Create a focused pull request that resolves this issue."      EXTRA_COMMIT_GITHUB_TOKEN: ${{ secrets.EXTRA_COMMIT_GITHUB_TOKEN }}
